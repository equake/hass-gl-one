# Test dependencies. pytest-homeassistant-custom-component pins a Home Assistant
# version; the packages below are that HA's `bluetooth`/`usb` component
# requirements, needed so the tests can import `homeassistant.components.bluetooth`.
# Keep these in sync with your HA version's manifests if you bump phcc.
pytest-homeassistant-custom-component

bleak==0.22.3
bleak-retry-connector==3.6.0
bluetooth-adapters==0.20.2
bluetooth-auto-recovery==1.4.2
bluetooth-data-tools==1.20.0
dbus-fast==2.24.3
habluetooth==3.7.0
pyserial==3.5
pyudev==0.24.1
